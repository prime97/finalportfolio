import tw, {styled} from "twin.macro";

export const ImageDiv = styled.div(() =>[
    tw``,
]);

export const ProfileImage = styled.img(() =>[
    tw`w-32 mx-auto shadow-xl rounded-full drop-shadow-sm`,
]);